package finn.creates.pumpkin;

/**
 * This can be NONE, SET_ACTIVE, CHALLENGE, or ENABLE_DISABLE. Used to set the form that the enable/disable command will show as.
 * @author Finn
 */
public enum ActiveCommandName {
    NONE,
    SET_ACTIVE,
    //CHALLENGE,
    ENABLE_DISABLE,
}